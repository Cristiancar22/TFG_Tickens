import { Text, View } from 'react-native'

export const CameraScreen = () => {
	return (
		<View className="flex-1 justify-center items-center">
			<Text className="text-xl">Sección de Camara</Text>
		</View>
	);
}

export default CameraScreen;